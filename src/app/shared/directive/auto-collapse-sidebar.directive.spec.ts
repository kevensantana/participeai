import { AutoCollapseSidebarDirective } from './auto-collapse-sidebar.directive';

describe('AutoCollapseSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCollapseSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
